import { TheaterMapDirective } from './theater-map.directive';

describe('TheaterMapDirective', () => {
  it('should create an instance', () => {
    const directive = new TheaterMapDirective();
    expect(directive).toBeTruthy();
  });
});
